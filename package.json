{
  "name": "dataface",
  "description": "Build and manage data with a spreadsheet-like interface",
  "author": "timwis <tim@timwis.com>",
  "private": true,
  "scripts": {
    "start": "budo src/main.js:dist/build.js --live -- -t localenvify",
    "build": "NODE_ENV=production browserify -t localenvify src/main.js | uglifyjs -c warnings=false -m > dist/build.js"
  },
  "dependencies": {
    "vue": "^2.0.1"
  },
  "devDependencies": {
    "browserify": "^13.0.1",
    "budo": "^10.0.3",
    "localenvify": "^1.0.1",
    "uglify-js": "^2.5.0",
    "vueify": "^9.1.0"
  },
  "browserify": {
    "transform": [
      "vueify"
    ]
  }
}
